-- Drops the sample_db if it exists currently --
DROP DATABASE IF EXISTS sample_db;
-- don't use DROP DATABASE too often. it is dangerous.
-- Creates the sample_db database --
CREATE DATABASE sample_db;
